extends layout

block content
  h1= title
  p Type any query below, we will return emails for that query.
  p
    strong How this works?
    |  We will search google with a given query. We will open the top 5 results and scrape each page for email addresses.
  .connection-info
  .form-row.align-items-center
    .col-auto
      label.sr-only(for='inlineFormInput') Name
      input#inlineFormInput.form-control.mb-2(type='text', onChange='onChangeString(event)', value='oftn inc')
    .col-auto
      button.btn.btn-primary.mb-2(onclick="window.clear()") Clear

  .progress
    .progress-bar(role='progressbar', style='width: 25%;', aria-valuenow='25', aria-valuemin='0', aria-valuemax='100') 25%
  table.table
    thead
      tr
        th(scope='col') #
        th(scope='col') First
        th(scope='col') Last
        th(scope='col') Handle
    tbody
      tr
        th(scope='row') 1
        td Mark
        td Otto
        td @mdo
      tr
        th(scope='row') 2
        td Jacob
        td Thornton
        td @fat
      tr
        th(scope='row') 3
        td Larry
        td the Bird
        td @twitter


  .res

